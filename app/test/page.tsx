import { AccessEntry } from "@/components/participant/access-entry";

/** Spec §18: `/test` is the participant's code-entry door. The token routes live beneath it. */
export default function TestEntryPage() {
  return <AccessEntry />;
}
